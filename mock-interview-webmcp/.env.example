# Optional. When present, the /api routes use OpenAI to generate
# questions and evaluate answers. When absent, the app falls back
# to a curated question bank + rubric-based feedback so the demo
# always works.
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
